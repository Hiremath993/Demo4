import PostsList from './components/PostsList';

function App() {
  return (
  <main>     
      <PostsList/>
  </main>             //<> </> : we can also use empty elements instead of <main>. These are so called React fragment.
  ); 
}

export default App;
